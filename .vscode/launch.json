{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Dev",
      "program": "${workspaceFolder}/index.js",
      "request": "launch",
      "command": "yarn start",
      "cwd": "${workspaceFolder}"
    }
  ]
}